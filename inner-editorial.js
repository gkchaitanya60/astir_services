(()=>{'use strict';const input=document.querySelector('#directory-search'),list=document.querySelector('[data-filter-list]'),status=document.querySelector('.filter-status');if(input&&list){const cards=[...list.querySelectorAll('.visual-entry')];function filter(){let count=0;const q=input.value.trim().toLowerCase();cards.forEach(c=>{c.hidden=q!==''&&!c.textContent.toLowerCase().includes(q);if(!c.hidden)count++});status.textContent=count+' '+(count===1?'result':'results')+(q?' for “'+input.value.trim()+'”':'')}input.addEventListener('input',filter);filter()}if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-seen');observer.unobserve(e.target)}}),{threshold:.07});document.querySelectorAll('[data-reveal]').forEach(e=>observer.observe(e))}})();
