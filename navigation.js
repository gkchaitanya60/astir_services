(()=>{'use strict';const button=document.querySelector('.menu-toggle'),nav=document.querySelector('#main-navigation');if(!button||!nav)return;const groups=[...document.querySelectorAll('.nav-dropdown')];function close(except){groups.forEach(g=>{if(g===except)return;g.querySelector('.dropdown-toggle').setAttribute('aria-expanded','false');g.querySelector('.dropdown-panel').hidden=true})}function menu(open){nav.classList.toggle('is-open',open);button.setAttribute('aria-expanded',String(open));button.querySelector('.sr-only').textContent=open?'Close navigation':'Open navigation';if(!open)close()}button.addEventListener('click',()=>menu(button.getAttribute('aria-expanded')!=='true'));groups.forEach(g=>{const b=g.querySelector('.dropdown-toggle'),p=g.querySelector('.dropdown-panel');b.addEventListener('click',()=>{const open=p.hidden;close(g);p.hidden=!open;b.setAttribute('aria-expanded',String(open))});g.addEventListener('focusout',e=>{if(!g.contains(e.relatedTarget)){p.hidden=true;b.setAttribute('aria-expanded','false')}})});document.addEventListener('click',e=>{if(!e.target.closest('.nav-dropdown'))close();if(!e.target.closest('.site-header'))menu(false)});document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;const g=groups.find(g=>!g.querySelector('.dropdown-panel').hidden);if(g){close();g.querySelector('button').focus()}else if(nav.classList.contains('is-open')){menu(false);button.focus()}});matchMedia('(min-width:1201px)').addEventListener('change',()=>menu(false));})();
